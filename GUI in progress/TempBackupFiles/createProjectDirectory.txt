if(QDir(QString("ProjectFiles/").append(newName)).exists()){
        alert.setText(QString("Project name ") + newName + " already exists!");
        alert.setInformativeText("Please try again");
        alert.setStandardButtons(QMessageBox::Close);
        alert.show();


    }else{
        QDir().mkdir(QString("ProjectFiles/").append(newName));
        saved = true;
        projectName = newName;
        ui->actionSave->setEnabled(true);
    }